# Bun configuration file
# This project uses Bun as the runtime and package manager

[install]
# Use Bun's fast install
auto = true

# Optional: configure registry if needed
# registry = "https://registry.npmjs.org/"

